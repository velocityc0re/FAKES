(function() {
    const shadowConfig = {
        name: "Edmentum Courseware",
        icon: "https://learner.ui.apps.elf.edmentum.com/2026.2.27.2-rel/secondary/favicon.ico",
        logo: "https://auth.edmentum.com/favicon.ico"
    };

    const coreHTML = `
    <!DOCTYPE html>
    <html lang="en">
    <head>
        <title>Loading State...</title>
        <link id="dynamic-fav" rel="icon" href="https://3kh0.github.io/img/logo.png">
        <style>
            body, html { margin:0; padding:0; height:100%; width:100%; overflow:hidden; background:#000; color:#0f0; font-family:monospace; }
            #boot-loader { display:flex; flex-direction:column; align-items:center; justify-content:center; height:100%; }
            #dashboard { display:none; width:100%; height:100%; background:#f4f7f9; position:fixed; inset:0; font-family: 'Segoe UI', Tahoma, sans-serif; }
            .sidebar { width:64px; height:100%; background:#003057; position:absolute; left:0; z-index:10; }
            .top-bar { height:56px; background:#fff; width:100%; border-bottom:1px solid #dee2e6; display:flex; align-items:center; padding-left:84px; box-sizing:border-box; }
            .content-area { padding: 40px 0 0 104px; color: #333; }
            .course-card { background:#fff; border-radius:8px; border:1px solid #ddd; padding:20px; width:340px; border-top:4px solid #0070c0; box-shadow: 0 2px 8px rgba(0,0,0,0.05); }
            .progress-bg { background:#e9ecef; border-radius:10px; height:8px; margin:15px 0; overflow:hidden; }
            .progress-fill { background:#28a745; height:100%; width:0%; transition:width 3s ease-in-out; }
        </style>
    </head>
    <body>
        <div id="boot-loader">
            <h1 style="letter-spacing:10px;">SYSTEM_ACTIVE</h1>
            <p>BYPASS_ENGINE: INJECTED</p>
            <p style="color:#333;">[ESC] TO SWITCH TO STEALTH MODE</p>
        </div>

        <div id="dashboard">
            <div class="sidebar"></div>
            <div class="top-bar">
                <div style="font-weight:600; color:#003057; display:flex; align-items:center;">
                    <img src="${shadowConfig.logo}" width="18" style="margin-right:10px;"> Courseware
                </div>
            </div>
            <div class="content-area">
                <h1 style="font-weight:300; margin-bottom:5px;">Home</h1>
                <p style="color:#666; font-size:14px; margin-bottom:25px;">Syncing your latest activity...</p>
                <div class="course-card">
                    <div style="font-weight:600; color:#003057;">Algebra 2: Semester B</div>
                    <div class="progress-bg"><div id="bar-fill" class="progress-fill"></div></div>
                    <div style="display:flex; justify-content:space-between; font-size:11px; color:#999;">
                        <span>Grade: 88.4%</span><span id="pct-text">0%</span>
                    </div>
                </div>
            </div>
        </div>

        <script>
            // Extension Stall Engine: Slows down extension scanning
            function pulseStall() {
                const start = Date.now();
                while (Date.now() - start < 150) { Math.sqrt(Math.random()); }
            }

            document.addEventListener("keydown", (e) => {
                if (e.key === "Escape") {
                    document.title = "${shadowConfig.name}";
                    document.getElementById("dynamic-fav").href = "${shadowConfig.icon}";
                    document.getElementById("boot-loader").remove();
                    document.getElementById("dashboard").style.display = "block";
                    document.body.style.background = "#f4f7f9";
                    
                    setTimeout(() => {
                        document.getElementById("bar-fill").style.width = "74%";
                        document.getElementById("pct-text").innerText = "74%";
                    }, 500);

                    // Run the stall pulse every 5 seconds to lag monitoring extensions
                    setInterval(pulseStall, 5000);
                }
            });
        <\/script>
    </body>
    </html>`;

    // Create the Blob and open in new tab to sever CSP ties
    const blob = new Blob([coreHTML], { type: 'text/html' });
    const blobURL = URL.createObjectURL(blob);
    const stealthWin = window.open(blobURL, '_blank');

    if (stealthWin) {
        console.log("%c SHADOW PROTOCOL v18.2 ACTIVATED ", "background: #000; color: #0f0; font-size: 20px; font-weight: bold;");
        console.log("Status: Mirror Running in Blob space. Press [ESC] in the new tab.");
    } else {
        alert("CRITICAL ERROR: Pop-up blocked. Please enable pop-ups to bypass the security policy.");
    }
})();
